# Neuro Dashboard Environment Variables

# API endpoint (proxied via Next.js rewrites)
NEXT_PUBLIC_API_URL=http://localhost:8000

# WebSocket endpoint
NEXT_PUBLIC_WS_URL=ws://localhost:8000/ws

# Prometheus endpoint
NEXT_PUBLIC_PROMETHEUS_URL=http://localhost:9090

# Enable mock data (set to "true" for development without pipeline)
NEXT_PUBLIC_USE_MOCK=true
